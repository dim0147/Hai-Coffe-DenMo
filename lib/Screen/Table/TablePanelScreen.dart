import 'package:flutter/material.dart';

class TablePanelScreen extends StatelessWidget {
  const TablePanelScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
